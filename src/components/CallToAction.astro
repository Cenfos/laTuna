---
const linkToContact = "/contacto";
---

<section class="call-to-action-container">
  <div class="call-to-action-card">
    <div class="call-to-action-content">
      <h2>¡Contáctanos!</h2>
      <p>
        Para cualquier consulta o para contratar nuestros servicios, no dudes en llamarnos.
      </p>
      <a href={linkToContact} class="call-to-action-button">
        Llamar
      </a>
    </div>
    <div class="call-to-action-img">
      <img src="/images/movil.png" alt="Ícono de Teléfono" />
    </div>
  </div>
</section>

<style>
  .call-to-action-container {
    display: flex;
    justify-content: center;
    align-items: center;
    padding: 40px 0;
  }

  .call-to-action-card {
    display: flex;
    justify-content: space-between;
    align-items: center;
    background: #f6f6f6;
    border: 5px solid #000;
    box-shadow: 10px 10px 0 #000;
    padding: 20px;
    width: 100%;
    max-width: 900px;
    border-radius: 8px;
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }

  .call-to-action-card:hover {
    transform: translate(-5px, -5px);
    box-shadow: 15px 15px 0 #000;
  }

  .call-to-action-content {
    max-width: 50%;
  }

  .call-to-action-content h2 {
    font-size: 2rem; /* Ajustado para mejor visualización en móviles */
    margin-bottom: 10px; /* Espacio reducido */
    color: #000;
    font-weight: 900;
  }

  .call-to-action-content p {
    font-size: 1rem; /* Ajustado para mejor legibilidad en móviles */
    margin-bottom: 15px; /* Espacio reducido */
    color: #000;
    font-weight: 500;
  }

  .call-to-action-button {
    display: inline-block;
    background: #ea1010;
    color: #fff;
    text-transform: uppercase;
    font-weight: 700;
    font-size: 1rem; /* Ajustado para mejor visualización en móviles */
    padding: 10px 20px; /* Espaciado reducido */
    text-align: center;
    border: 5px solid #000;
    box-shadow: 5px 5px 0 #000;
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }

  .call-to-action-button:hover {
    transform: translate(-3px, -3px);
    box-shadow: 8px 8px 0 #000;
  }

  .call-to-action-img img {
    max-width: 100%; /* Asegura que la imagen no exceda el contenedor */
    height: auto; /* Mantiene la proporción de la imagen */
  }

  /* Estilos para dispositivos móviles */
  @media (max-width: 768px) {
    .call-to-action-card {
      flex-direction: column; /* Cambia a columna en móviles */
      align-items: center; /* Centra los elementos */
      padding: 20px; /* Espaciado interno reducido */
    }

    .call-to-action-content {
      max-width: 100%; /* Asegura que el contenido ocupe el ancho completo */
      text-align: center; /* Centra el texto en móviles */
    }

    .call-to-action-img {
      max-width: 100%; /* Asegura que la imagen no exceda el contenedor */
      margin-top: 10px; /* Espaciado superior para separación */
    }
  }
</style>
